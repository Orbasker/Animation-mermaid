# Fixture for the secret-scanning self-test. These are NOT real credentials —
# they exist so CI can prove gitleaks rejects a committed secret. The repo scan
# allowlists this file (see .gitleaks.toml); the self-test scans it without the
# allowlist and MUST find the leak. Never put a real secret here.
AWS_ACCESS_KEY_ID=AKIAZ3XMPLE7QK2NR8TQ
AWS_SECRET_ACCESS_KEY=wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY
